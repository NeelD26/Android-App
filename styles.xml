<resources>
    <!-- Base application theme. -->
    <style name="AppTheme" parent="Base.V7.Theme.AppCompat">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/topbar</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>
    <style name="AppThemes" parent="android:Theme">
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowContentOverlay">@id/lstNames</item>
        <item name="android:windowBackground">@id/lstNames</item>
    </style>
</resources>